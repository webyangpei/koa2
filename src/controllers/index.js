const HomeController = require('./home');
const UsersController = require('./users');
// const articles = require('./articles');

module.exports = {
  HomeController,
  UsersController,
  // articles
}
